import 'package:flutter/material.dart';

class IconLabel {
  String label;
  IconData icon;
  Color iconColor;
  Color labelColor;

  IconLabel(this.label, this.icon, this.iconColor, this.labelColor);
}
